//Problem 4
#include<stdio.h>
int main()
{
    //Take two positive integers as input
    int x,y;
    scanf("%d %d",&x,&y);

    //Show the summation, subtraction, multiplication of those two numbers.
    printf("%d %d %d",x+y,x-y,x*y);
    return 0;
}
